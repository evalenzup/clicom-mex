# Backend FastAPI para Mapa de Estaciones

## Instrucciones

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

- Datos CSV van en: `app/data/csv`
- Catálogo JSON va en: `app/data/json`
